schema = "type A { id: ID! } type AError { message: String! } type AResult { payload: A! }"

[options]
acceptedIdNames = ["id"]
acceptedIdTypes = ["ID"]

[options.exceptions]
suffixes = ["Result"]
types = ["AError"]
