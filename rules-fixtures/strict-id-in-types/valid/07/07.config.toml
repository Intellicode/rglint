kind = "schema"
schema = "type A { id: ID! } type AResult { key: String! } type APayload { bool: Boolean! } type APagination { num: Int! }"

[options]
acceptedIdNames = ["id"]
acceptedIdTypes = ["ID"]

[options.exceptions]
suffixes = ["Result", "Payload", "Pagination"]
