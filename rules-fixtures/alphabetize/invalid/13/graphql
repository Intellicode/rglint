        type Test { # { character
          # before d 1

          # before d 2
          d: Int # same d
          # before c
          c: Float!
          # before b 1
          # before b 2
          b: [String] # same b
          # before a
          a: [Int!]! # same a
          # end
        } # } character
      