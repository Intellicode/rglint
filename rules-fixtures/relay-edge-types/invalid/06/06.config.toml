kind = "schema"
schema = """
        type User {
          id: ID!
        }
        type AEdge {
          node: User!
          cursor: String!
        }
        type AConnection {
          edges: [AEdge]
        }
      """

[options]
shouldImplementNode = true
